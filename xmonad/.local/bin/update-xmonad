#!/bin/bash

set -x

XMONAD_DIR="$HOME/.config/xmonad/"

# Pull repos
git -C "$XMONAD_DIR"/xmonad pull --rebase
git -C "$XMONAD_DIR"/xmonad-contrib pull --rebase

# Install xmonad
cd "$XMONAD_DIR" || exit
stack install

# Recompile xmonad config
xmonad --recompile
